import { StyleSheet } from 'react-native';

export const colors = {
  partnerRed: '#8D021F',
  selfGreen: '#2EBB57',
}

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
    alignItems: 'center',
    paddingTop: 80
  },
});